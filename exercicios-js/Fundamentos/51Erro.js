function imprimirNome(obj){// criei uma funcao imprimirNome, passei um parametro (obj),, dentro do objeto vou chamar....
    console.log(obj.name.toUpperCase() + '!!!')/// toUpperCase == converter as letras para maiusculo,, em seguida concatenar com esclamação.


}
//// try == trata o erro e joga pro cath
//// cath === cath resolve e joga pro throw.
///finally === sempre sera executado mesmo se o try tiver errado
//// throw == faz o processo..