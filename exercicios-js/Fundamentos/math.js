const raio = 5.6
const area = Math.PI * Math.pow(raio, 2) //PI= é uma constante de dentro de math / passando parametro raio elevado ao quadrado

console.log(area)
console.log(typeof math) // MATH = é um objeto.  // O POW = é o comando que calcula a potencia.