//1= const up = texto => texto.toUpperCase() // up e toUpperCase = pegar as letras e transaforma para maiusculo ..
//2= valores.push({id: 3}, false, null, 'teste' )// push=serve para adicionar novos elementos num arrey( false=booleano, null=nulo, string=teste)
//3= console.log(Math.ceil(6.1)) /// serve para operar um arredondamento  // Math =  é um objeto dentro do java
//4= this.nome = nome// this = é mesma coisa que criar um atributo ,, so que fica visivel para publico.
//5= const valor = Math.random()* (max-min)+min/// random:: é uma função de numero aleatória.
//6= return Math.floor(valor)/// floor :: arredonda o valor para baixo,,, ceil:: para cima
//7= || = operador tipo(ou);;;  && = tipo (e);; != =(diferente) ;;;; ? = (operador ternario);.; ++ = (acrescenta 1)
//8= console.log(obj.name.toUpperCase() + '!!!')/// toUpperCase == converter as letras para maiusculo,, em seguida concatenar com esclamação.
//9= try == trata o erro e joga pro cath
//10= cath === cath resolve e joga pro throw.
//11= finally === sempre sera executado mesmo se o try tiver errado
//12= throw == faz o processo..
//13 = Math.PI * Math.pow(raio, 2) //PI= é uma constante de dentro de math;; MATH = é um objeto.  // O POW = é o comando que calcula a potencia.
//14 = console.log(Number.isInteger(peso1))// isinteger = ve se o numero é inteiro se for (true ou false);
//15 = console.log(media.toFixed(2)); // ToFixed ==mostra o tanto de casas decimais que voce queira que mostra.
//16 = console.log(media.toString()); // ToString == retorna os valores decimais tirados do tofixed.
//17 = console.log(escola.charAt(4)) // significa que vai dar a letra no indice 4 na string = letra (r )
//18 = console.log(escola.charCodeAt(3)) // nessa parte ele pegou o valor da tabela asc  deu como resultado 101
//19 = console.log(escola.indexOf('3')) // irá dar o digito da palavra associada. = 3
//20 = console.log(escola.substring(1))// indice na tela a partir do valor 1.
//21 = console.log(escola.replace(3, "e")); // Replace = comando de substituiçaõ.  = substitui a letra.
//22 = console.log('Ana, Maria e Pedro'.split(',')); // Split= comando que gera um harrei,, entre cochetes.